- Agent 配置统一使用 LYL_ 前缀的环境变量（LYL_MODEL_PROVIDER / LYL_MODEL / LYL_MODEL_API_KEY / LYL_MODEL_BASE_URL / LYL_STUB_RESPONSE），且密钥仅放入 apps/agent/.env，禁止放入 NEXT_PUBLIC_* 变量。
- 所有开发、构建、校验命令均通过根 package.json 的 scripts 经 pnpm --filter @cos/web 转发，避免在各子包中重复定义入口。
- 上游开源代码（如 agent-chat-ui）保留原始 LICENSE 并在 NOTICE.md 中集中归因，修改需遵循 MIT 许可。