# Qoder offline snapshot

此目录是项目特意纳入版本控制的 Qoder 离线知识快照与 harness 报告，不是运行时
依赖，也不会被 Web 或 Agent 构建加载。

- `repowiki/`：生成的仓库知识页面。
- `better-harness/`：生成时的分析报告和可视化源文件。
- 源代码、测试和根文档始终是事实来源；快照内容与代码冲突时以代码为准。
- 更新快照时应重新核对文件引用、测试声明、协议和传输方式，避免把推测写成现状。

该快照包含 PR #18 的 Agent Graph 上下文，因此 PR #19 应在 PR #18 之后合并。

